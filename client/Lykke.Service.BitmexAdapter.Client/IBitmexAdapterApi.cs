using JetBrains.Annotations;
using Refit;

namespace Lykke.Service.BitmexAdapter.Client
{
    // This is an example of service controller interfaces.
    // Actual interface methods must be placed here (not in IBitmexAdapterClient interface).

    /// <summary>
    /// BitmexAdapter client API interface.
    /// </summary>
    [PublicAPI]
    public interface IBitmexAdapterApi
    {
    }
}
